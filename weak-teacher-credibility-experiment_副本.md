# 弱教师信号的可信度加权——实验方案

> 硬件约束：单卡 V100 16GB（FP16，无 BF16；7B 需 QLoRA，1.5B–3B 可全参）
> 体系背景：延续「弱师与无师」方向——Direct OPD[37]、Weak Critics[35]、It Takes 8 Tokens[38]、Strong Teacher Not Needed[39] 已证明弱教师信号有效，但均未系统回答「如何估计并利用弱教师信号的可信度」。本方案以此为研究空白。

---

## 1. 研究问题与假设

### 1.1 核心问题

弱教师（能力弱于学生、或与学生相当）提供蒸馏信号时，信号质量逐样本差异极大——部分样本信号接近强教师，部分样本信号接近噪声。现有方法把所有弱教师信号一视同仁，本方案研究：**如何估计每个样本（或每个 token）的可信度，并按可信度加权，使弱教师蒸馏的收益最大化、噪声污染最小化。**

### 1.2 可证伪假设

| 编号 | 假设 | 证伪条件 |
|---|---|---|
| H1 | 可信度加权的弱教师蒸馏优于不加权的弱教师蒸馏 | 加权后性能与不加权无显著差异（Δ<1%） |
| H2 | 可验证域中，规则验证器是最优可信度估计器 | 其他估计器（自报置信度/一致性）在可验证任务上显著更优 |
| H3 | 混合强弱教师时，按可信度加权优于按教师身份固定加权 | 固定加权（强师权重 1.0 / 弱师 0.5）不劣于动态可信度加权 |
| H4 | 可信度估计本身可被学生策略校准（on-policy 效应） | 用学生当前策略估计可信度与用教师静态估计无差异 |

### 1.3 预期贡献

- 提出一个统一的可信度加权蒸馏框架（估计器 + 加权机制可插拔）
- 在数学/代码两类任务、多档弱教师强度下给出系统对比
- 回答「弱教师信号在什么条件下值得用、怎么用」

---

## 2. 实验设置

### 2.1 基座与学生模型

| 用途 | 模型 | 参数量 | 训练方式 | 显存占用 |
|---|---|---|---|---|
| 学生（主实验） | Qwen2.5-1.5B-Instruct | 1.5B | 全参 SFT | ~14GB（batch 8，max_len 2048） |
| 学生（扩展） | Qwen2.5-3B-Instruct | 3B | 全参 SFT | 需梯度累积/减 batch |
| 学生（验证规模效应） | Qwen2.5-7B-Instruct | 7B | QLoRA 4bit | ~10GB |
| 教师池底座 | Qwen2.5-0.5B / 1.5B / 3B / 7B | 0.5–7B | 仅推理 | 无压力 |

### 2.2 弱教师池构造（可控降级）

弱教师的「弱」需要可控、可量化，采用四种构造方式形成梯度：

| 弱教师类型 | 构造方法 | 期望强度 | 说明 |
|---|---|---|---|
| 小规模教师 | Qwen2.5-0.5B / 1.5B（< 学生） | 明显弱 | 尺寸降级，最自然 |
| 截断训练教师 | 用学生底座在 20%/50%/100% 数据上各训一个 checkpoint | 渐变弱 | 同一底座，强度连续可调 |
| 加噪教师 | 对教师 logits 注入高斯噪声（σ=0.1/0.5/1.0） | 可控噪声 | 隔离「能力弱」与「信号噪」 |
| 混合教师池 | 上述教师按 1:1 混合采样 | 多源弱 | 模拟真实多教师场景 |

> 每个教师都在验证集上实测准确率，作为其「真实可信度基线」，用于验证估计器的校准性。

### 2.3 任务与数据集

| 域 | 训练数据 | 验证/测试 | 可验证性 |
|---|---|---|---|
| 数学 | GSM8K 训练集（教师生成 CoT + 答案） | GSM8K test、MATH500 | 规则判题器（答案匹配） |
| 数学进阶 | MATH train（抽样 20K） | MATH500、AIME24 | 规则判题器 |
| 代码 | HumanEval / MBPP prompts（教师生成 解答） | HumanEval、MBPP | 单元测试执行 |

> 数学为主实验域（可验证性最强，判题器廉价），代码为扩展域。所有教师轨迹统一用 `problem → CoT → answer` 格式，长度截断 2048。

---

## 3. 方法：可信度估计器 × 加权机制

### 3.1 可信度估计器（对每个训练样本输出一个标量置信度 c∈[0,1]）

| 编号 | 估计器 | 计算方式 | 信息源 | 成本 |
|---|---|---|---|---|
| E0 | 恒定 1.0（基线，不加权） | — | — | 0 |
| E1 | 教师自报置信度 | 教师生成时平均 token logprob 归一化 | 教师自身 | 低 |
| E2 | 教师自一致性 | 教师对同一问题采样 K=8 次，答案一致率 | 教师采样 | 中 |
| E3 | 规则验证器 | 数学：答案判题正确=1；代码：单测通过=1 | 外部规则 | 低（数学） |
| E4 | 教师-学生一致性 | 教师答案与学生当前策略答案是否一致 | 师生双方 | 中 |
| E5 | 多教师聚合 | N 个弱教师投票，多数占比作为可信度 | 教师池 | 中 |
| E6 | 混合估计器 | E3 为主，E1/E2 作为平滑项（可验证域） | 组合 | 中 |

> E4 是唯一利用学生当前策略的估计器，对应 H4 的 on-policy 假设，需要蒸馏循环中每轮重新估计。

### 3.2 加权机制（如何把 c 用进训练）

| 编号 | 机制 | 公式/行为 | 特点 |
|---|---|---|---|
| W0 | 无加权 | 标准 SFT loss | 基线 |
| W1 | 样本级重加权 | loss_i × c_i | 简单，c 连续 |
| W2 | 样本级选择性 | 保留 c>τ 的样本（τ 扫 0.5/0.7/0.9） | 硬阈值 |
| W3 | token 级重加权 | 每个 token 权重由该 token 对应位置的教师置信度决定 | 细粒度 |
| W4 | 软标签插值 | 目标分布 = c × onehot(教师答案) + (1−c) × uniform | 防过度自信 |
| W5 | 课程加权 | 先全量 W1（c>0.8），再逐步加入低 c 样本 | 课程学习 |

### 3.3 主实验矩阵

`估计器（7 种）× 加权机制（6 种）` 全组合为 42 组，但受算力限制，采用两阶段筛选：

**阶段 A（粗筛，1.5B 学生 + 截断 50% 教师）**：所有 42 组合各训 1 个 epoch（GSM8K 8K 样本），MATH500 评测。选出 top 3 估计器 × top 3 机制（9 组）。

**阶段 B（精测，1.5B 学生 + 全教师梯度）**：9 组 × 4 类弱教师 × 2 任务（数学/代码），训满 3 epoch，全基准评测。

---

## 4. 评测指标

| 指标 | 定义 | 用途 |
|---|---|---|
| 准确率 | GSM8K / MATH500 / HumanEval pass@1 | 主指标 |
| 相对增益 | (加权 − 无加权) / 无加权 | 判断加权是否有用（H1） |
| 校准误差 ECE | 可信度 c 与「样本是否被教师答对」的校准 | 估计器质量（H2） |
| 教师强度鲁棒性 | 教师从强→弱（截断 100%→20%），性能下降曲线 | 加权的降级保护 |
| 噪声容忍度 | 教师 logits 加噪 σ=0.1/0.5/1.0 时性能保持率 | 隔离信号噪声 |
| 训练效率 | 达到目标准确率的 epoch 数 / 总 token 数 | 算力成本对比 |

---

## 5. 消融与机制分析

1. **估计器消融**：E1–E6 单独 vs 组合，找最小充分估计器集
2. **加权机制消融**：W1–W5 在同一估计器（E3）下对比
3. **教师强度×估计器交互**：不同弱教师强度下，哪个估计器最鲁棒（关键图）
4. **学生规模效应**：0.5B 学生（最弱）vs 3B 学生，加权收益是否随学生变强而衰减
5. **on-policy 效应**：E4 在多轮蒸馏中是否越用越准（H4，需要 2 轮迭代）
6. **可信度 vs 难度**：按问题难度分层（MATH 难度分级），分析加权收益集中在哪个难度层

---

## 6. 实验排期与算力估算

以 V100 16GB 单卡为基准估算（1.5B 全参 SFT，8K 样本，max_len 2048）：

| 阶段 | 内容 | 训练组数 | 单组时长 | 总 GPU 天 |
|---|---|---|---|---|
| A 粗筛 | 42 组合 × 1 epoch | 42 | ~1.5h | ~2.5 |
| B 精测 | 9 组 × 4 教师 × 2 域 × 3 epoch | 72 | ~4h | ~12 |
| 消融 | 约 20 组补充 | 20 | ~2h | ~1.5 |
| 教师构造 | 截断教师 3 个 + 采样 | — | — | ~1 |
| 合计 | — | — | — | **约 17 GPU 天** |

> 若仅有碎片时间，可先只做阶段 A 的 42 组 + 数学域，约 4 天可产出第一版结论。

---

## 7. 风险与对策

| 风险 | 说明 | 对策 |
|---|---|---|
| 弱教师信号太差，加权收益被淹没 | 最弱教师（0.5B）可能在 GSM8K 上仅 ~20% 正确 | 弱教师池保底用截断 50% 教师（期望 ~50%），0.5B 只作极端点 |
| 规则验证器主导一切，其他估计器显得无价值 | 数学域可验证性天然偏向 E3 | 代码域（单测）对照 + 加入「教师答错但接近正确」的软信号分析 |
| 学生过拟合教师错误模式 | 弱教师系统性错误被加权放大 | W4 软标签插值 + 在 H1 中对比「加权 vs 数据过滤（只留教师答对）」 |
| V100 显存瓶颈 | 7B QLoRA 上长 CoT 显存吃紧 | 主实验锁 1.5B，7B 只做验证性一组 |
| 可信度估计引入额外推理成本 | E2/E5 需多次采样 | 报告「估计成本 vs 收益」曲线，说明算力权衡 |

---

## 8. 复现链与开源对标

- 训练框架：HuggingFace TRL SFT 或 open-r1（ReasonLite 同款代码库），便于直接对标
- 数据管线：复用 ReasonLite 的「生成 → 判题 → 过滤」脚本，改造为「生成 → 估计可信度 → 加权训练」
- 基线对标：无加权弱教师蒸馏 = 最强基线；强教师蒸馏（Qwen2.5-7B 作教师）为上界参考
- 交付物：估计器/加权机制各 6 种的可插拔实现、评测脚本、MATH500/AIME24/HumanEval 全结果表
