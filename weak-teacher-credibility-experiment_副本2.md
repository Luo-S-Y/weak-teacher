# 弱教师信号的可信度加权——实验方案（v2：纯 logits 蒸馏）

> 硬件约束：单卡 V100 16GB（FP16，无 BF16；7B 需 QLoRA，1.5B–3B 可全参）
> 体系背景：延续「弱师与无师」方向——Direct OPD[37]、Weak Critics[35]、It Takes 8 Tokens[38]、Strong Teacher Not Needed[39] 已证明弱教师信号有效，但均未系统回答「如何估计并利用弱教师信号的可信度」。本方案以此为研究空白。
> v2 变更：训练侧**不使用规则判题**，教师仅提供**逐 token 概率分布（logits）**作为监督信号；训练数据完全来自**学生自采样轨迹（on-policy rollout）**，与 ESR[31] 的 reverse-KL 设置一致。评测侧仍用标准基准判对错（报告准确率，二者互不影响）。

---

## 1. 研究问题与假设

### 1.1 核心问题

弱教师（能力弱于学生、或与学生相当）提供逐 token 概率信号时，信号质量逐样本、逐 token 差异极大——部分位置的概率分布接近强教师，部分位置接近噪声甚至误导。现有方法（包括 ESR）对所有 token 位置一视同仁地做 KL 蒸馏，本方案研究：**如何估计每个样本（或每个 token）的教师概率分布可信度，并按可信度加权，使弱教师 logits 蒸馏的收益最大化、噪声污染最小化。**

### 1.2 可证伪假设

| 编号 | 假设 | 证伪条件 |
|---|---|---|
| H1 | 可信度加权的弱教师 logits 蒸馏优于不加权（ESR 式均匀 KL） | 加权后性能与不加权无显著差异（Δ<1%） |
| H2 | logits 内在信号（分布熵/集中度）能有效代理可信度，且优于教师自报置信度 | 分布熵估计器与自报置信度在加噪教师下无显著差异 |
| H3 | 混合强弱教师时，按可信度加权优于按教师身份固定加权 | 固定加权（强师权重 1.0 / 弱师 0.5）不劣于动态可信度加权 |
| H4 | 可信度估计本身可被学生策略校准（on-policy 效应） | 用学生当前策略估计可信度与用教师静态估计无差异 |

### 1.3 预期贡献

- 提出一个不依赖规则判题、纯 logits 的可信度加权蒸馏框架（估计器 + 加权机制可插拔）
- 在数学/代码两类任务、多档弱教师强度下给出系统对比
- 回答「弱教师 logits 信号在什么条件下值得用、怎么用」

---

## 2. 实验设置

### 2.1 基座与学生模型

| 用途 | 模型 | 参数量 | 训练方式 | 显存占用 |
|---|---|---|---|---|
| 学生（主实验） | Qwen2.5-1.5B-Instruct | 1.5B | LoRA（r=32, α=64） | ~10GB（batch 16，max_len 2048） |
| 学生（扩展） | Qwen2.5-3B-Instruct | 3B | LoRA | 需梯度累积/减 batch |
| 学生（验证规模效应） | Qwen2.5-7B-Instruct | 7B | QLoRA 4bit | ~10GB |
| 教师池底座 | Qwen2.5-0.5B / 1.5B / 3B / 7B | 0.5–7B | 仅推理（需输出 logits） | 无压力 |

### 2.2 弱教师池构造（可控降级）

弱教师的「弱」需要可控、可量化，采用四种构造方式形成梯度。**所有教师均需提供逐 token 概率分布**（softmax 前 logits）：

| 弱教师类型 | 构造方法 | 期望强度 | 说明 |
|---|---|---|---|
| 小规模教师 | Qwen2.5-0.5B / 1.5B（< 学生） | 明显弱 | 尺寸降级，最自然 |
| 截断训练教师 | 用学生底座在 20%/50%/100% 数据上各训一个 checkpoint | 渐变弱 | 同一底座，强度连续可调 |
| 加噪教师 | 对教师 logits 注入高斯噪声（σ=0.1/0.5/1.0） | 可控噪声 | 隔离「能力弱」与「信号噪」，直接作用于监督信号 |
| 混合教师池 | 上述教师按 1:1 混合采样 | 多源弱 | 模拟真实多教师场景 |

> 每个教师都在验证集上实测准确率（评测判题，仅用于刻画教师强度梯度与校准性验证），但**训练循环内不调用判题器**。

### 2.3 任务与数据集

| 域 | 问题池（rollout 输入） | 评测 | 说明 |
|---|---|---|---|
| 数学 | GSM8K 训练集（7473 题） | MATH500、AIME24 | 主实验域 |
| 数学进阶 | MATH train（抽样 20K） | MATH500、AIME24 | 扩展 |
| 代码 | HumanEval / MBPP prompts | HumanEval、MBPP | 扩展域 |

> 问题池只提供**输入**；监督信号全部来自学生 rollout + 教师逐 token 概率，训练侧无任何判题。评测侧用标准基准判对错报告准确率。

---

## 3. 方法：训练循环、可信度估计器 × 加权机制

### 3.1 训练循环（on-policy，学生产生轨迹）

与 ESR 一致的 reverse-KL 蒸馏循环，核心差异是**逐样本/逐 token 可信度加权**：

| 步骤 | 操作 | 细节 |
|---|---|---|
| ① 采样问题 | 从问题池抽 batch 16 题 | 静态输入 |
| ② 学生 rollout | 学生 πθ 对每题生成轨迹 ŷ（n=1，温度 0.7，截断 N=100） | **轨迹由学生产生** |
| ③ 教师逐 token 概率 | 弱教师对每条学生轨迹的前缀逐位置输出概率分布 q_t = πT(·\|x, ŷ< t) | 纯 logits 监督 |
| ④ 估计可信度 | 用估计器对每个样本/token 算权重 c | 见 3.2 |
| ⑤ 加权更新 | L = Σ c·KL(πθ(·\|x,ŷ<t) \|\| πT(·\|x,ŷ<t))，更新学生 | 回到 ① |

> 样本 = `(问题 x, 学生轨迹 ŷ, 教师逐 token 分布 q, 可信度权重 c)`。第 ② 步轨迹来自更新后的学生，即 on-policy；每轮数据不可复用。与 ESR 唯一的结构差异在第 ④ 步加权。

### 3.2 可信度估计器（输出标量 c∈[0,1] 或逐 token c_t，全部基于 logits，不依赖判题）

| 编号 | 估计器 | 计算方式 | 信息源 | 成本 |
|---|---|---|---|---|
| E0 | 恒定 1.0（基线，ESR 式均匀 KL） | — | — | 0 |
| E1 | 教师自报置信度 | 教师分布的平均 token logprob 归一化 | 教师自身 | 低 |
| E2 | 教师分布集中度 | 教师 top-k 分布熵/概率质量集中度（熵越低越可信） | 教师分布 | 低 |
| E3 | 教师分布尖锐度 | 教师 top-1 概率 vs top-2 差距（越尖锐越可信） | 教师分布 | 低 |
| E4 | 教师-学生分布重叠 | 教师与学生 top-k 分布重叠率（overlap ratio，Rethinking OPD 机制指标） | 师生双方 | 中 |
| E5 | 多教师聚合一致性 | 多个弱教师分布两两 KL 距离（越小越可信） | 教师池 | 中 |
| E6 | 混合估计器 | E1/E2 归一化后加权组合 | 组合 | 低 |

> E4 是唯一利用学生当前策略的估计器，对应 H4 的 on-policy 假设，需要在蒸馏循环中每轮重新估计（与第 ② 步共用前向结果，额外成本低）。

### 3.3 加权机制（如何把 c 用进 KL 蒸馏）

| 编号 | 机制 | 公式/行为 | 特点 |
|---|---|---|---|
| W0 | 无加权 | 标准 reverse-KL（ESR 同款） | 基线 |
| W1 | 样本级重加权 | KL_i × c_i | 简单，c 连续 |
| W2 | 样本级选择性 | 保留 c>τ 的样本（τ 扫 0.5/0.7/0.9） | 硬阈值 |
| W3 | token 级重加权 | 每个 token 的 KL 项乘该位置 c_t | 细粒度 |
| W4 | 分布插值 | 目标分布 = c × q_t + (1−c) × uniform | 防过度自信 |
| W5 | 课程加权 | 先全量 W1（c>0.8），再逐步加入低 c 样本 | 课程学习 |

### 3.4 主实验矩阵

`估计器（7 种）× 加权机制（6 种）` 全组合为 42 组，受算力限制采用两阶段筛选：

**阶段 A（粗筛，1.5B 学生 + 截断 50% 教师）**：42 组合各训 200 步（16 题/步 × 200 步 = 3,200 题用量，ESR 同规模），MATH500 评测。选出 top 3 估计器 × top 3 机制（9 组）。

**阶段 B（精测，1.5B 学生 + 全教师梯度）**：9 组 × 4 类弱教师 × 2 任务（数学/代码），训满 600 步，全基准评测。

---

## 4. 评测指标

| 指标 | 定义 | 用途 |
|---|---|---|
| 准确率 | MATH500 / AIME24 / HumanEval pass@1 | 主指标（评测判题，与训练侧无关） |
| 相对增益 | (加权 − 不加权) / 不加权 | 判断加权是否有用（H1） |
| 校准误差 ECE | 可信度 c 与「教师分布质量」的校准（如与 logits 加噪水平的单调关系） | 估计器质量（H2） |
| 教师强度鲁棒性 | 教师从强→弱（截断 100%→20%），性能下降曲线 | 加权的降级保护 |
| 噪声容忍度 | 教师 logits 加噪 σ=0.1/0.5/1.0 时性能保持率 | 隔离信号噪声 |
| 训练效率 | 达到目标准确率的步数 / 总 token 数 | 算力成本对比 |

---

## 5. 消融与机制分析

1. **估计器消融**：E1–E6 单独 vs 组合，找最小充分估计器集
2. **加权机制消融**：W1–W5 在同一估计器（E2）下对比
3. **教师强度×估计器交互**：不同弱教师强度下，哪个估计器最鲁棒（关键图）
4. **学生规模效应**：0.5B 学生（最弱）vs 3B 学生，加权收益是否随学生变强而衰减
5. **on-policy 效应**：E4 在多轮蒸馏中是否越用越准（H4，需要 2 轮迭代）
6. **可信度 vs 轨迹位置**：按 token 位置（前段 vs 后段）分层分析加权收益，验证是否与 ESR 的「前段更有用」一致

---

## 6. 实验排期与算力估算

以 V100 16GB 单卡为基准估算（1.5B LoRA，batch 16，max_len 2048）：

| 阶段 | 内容 | 训练组数 | 单组时长 | 总 GPU 天 |
|---|---|---|---|---|
| A 粗筛 | 42 组合 × 200 步 | 42 | ~1.5h | ~2.5 |
| B 精测 | 9 组 × 4 教师 × 2 域 × 600 步 | 72 | ~4h | ~12 |
| 消融 | 约 20 组补充 | 20 | ~2h | ~1.5 |
| 教师构造 | 截断教师 3 个 + 采样 | — | — | ~1 |
| 合计 | — | — | — | **约 17 GPU 天** |

> 若仅有碎片时间，可先只做阶段 A 的 42 组 + 数学域，约 4 天可产出第一版结论。

---

## 7. 风险与对策

| 风险 | 说明 | 对策 |
|---|---|---|
| 去掉判题后可信度失去「对错锚点」 | 训练侧无正确性标签，估计器完全依赖 logits 内在信号 | H2 专门验证 logits 信号能否代理可信度；若 E2/E3 无效则结论明确为「需要外部信号」，仍是可发表的负面结果 |
| 弱教师 logits 分布过于扁平/错误 | 最弱教师（0.5B）分布可能接近均匀，KL 信号弱 | 弱教师池保底用截断 50% 教师，0.5B 只作极端点 |
| 学生过拟合教师分布模式 | 弱教师分布中的系统性偏差被加权放大 | W4 分布插值（c×q + (1−c)×uniform）作为正则 |
| V100 显存瓶颈 | 需同时跑学生前向 + 教师前向（双模型） | 主实验锁 1.5B LoRA；教师用 FP16 推理，显存 ~5GB |
| 可信度估计引入额外推理成本 | E4/E5 需额外前向 | E4 复用第 ② 步学生前向结果；报告「估计成本 vs 收益」曲线 |

---

## 8. 复现链与开源对标

- 训练框架：以 ESR（arXiv 2605.27028）的 reverse-KL 蒸馏循环为基座实现（Loss = Σ KL(学生‖教师)），在其上叠加可信度加权层
- 数据管线：问题池用公开数据集（GSM8K / MATH / HumanEval prompts）；监督信号全部在线生成（学生 rollout + 教师 logits）
- 基线对标：不加权 reverse-KL（ESR 同款）= 最强基线；强教师（Qwen2.5-7B）同设置 = 上界参考
- 交付物：估计器/加权机制各 6 种的可插拔实现、训练循环脚本、MATH500/AIME24/HumanEval 全结果表、可信度-性能校准曲线
